<%= simple_form_for([@user, @setting]) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :private %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
