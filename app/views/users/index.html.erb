<h3>Users</h3>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %> signed up <%= user.created_at.to_date %> <%= link_to 'Follow', follow_user_path(user, :format => :json), remote: true, method: 'post' %>
      
    </li>
  <% end %>
</ul>
